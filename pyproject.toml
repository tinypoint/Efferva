[project]
name = "agentframe"
version = "0.1.0"
description = "A Codex-based, AG-UI compatible agent framework for Docker and Kubernetes."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.12,<4",
    "fastapi>=0.116,<1",
    "kubernetes-asyncio>=33,<34",
    "psycopg[binary,pool]>=3.2,<4",
    "pydantic-settings>=2.10,<3",
    "uvicorn[standard]>=0.35,<1",
    "websockets>=15,<16",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.4,<9",
    "pytest-asyncio>=1.1,<2",
    "ruff>=0.12,<1",
]

[project.scripts]
agentframe = "agentframe.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agentframe"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "integration: requires a PostgreSQL instance and built runtime",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]
