diff --git a/agent/api_server.py b/agent/api_server.py
index e284587..ec3b082 100644
--- a/agent/api_server.py
+++ b/agent/api_server.py
@@ -191,2 +191,10 @@ from src.api.runs_routes import (  # noqa: F401, E402
-from src.api.sessions_routes import register_sessions_routes  # noqa: E402
-register_sessions_routes(app)
+from src.config.accessor import get_env_config  # noqa: E402
+
+if get_env_config().api.agent_runtime.strip().lower() == "efferva":
+    from src.efferva_product import install_efferva_product  # noqa: E402
+
+    install_efferva_product(app)
+else:
+    from src.api.sessions_routes import register_sessions_routes  # noqa: E402
+
+    register_sessions_routes(app)
diff --git a/agent/src/config/env_schema.py b/agent/src/config/env_schema.py
index a3fb8e2..7ef8b99 100644
--- a/agent/src/config/env_schema.py
+++ b/agent/src/config/env_schema.py
@@ -244,0 +245,11 @@ class APIConfig(_EnvBase):
+    agent_runtime: str = Field(alias="VIBE_TRADING_AGENT_RUNTIME", default="native")
+    product_auth_mode: str = Field(alias="VIBE_TRADING_PRODUCT_AUTH_MODE", default="dev")
+    product_tenant_id: str = Field(
+        alias="VIBE_TRADING_PRODUCT_TENANT_ID",
+        default="vibe-trading",
+    )
+    product_dev_user: str = Field(alias="VIBE_TRADING_PRODUCT_DEV_USER", default="alice")
+    product_iap_audience: str = Field(
+        alias="VIBE_TRADING_PRODUCT_IAP_AUDIENCE",
+        default="",
+    )
diff --git a/pyproject.toml b/pyproject.toml
index 366c333..c8d4a57 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -96,0 +97,4 @@ include = ["src*", "backtest*", "cli*"]
+efferva = [
+    "efferva[opensandbox]>=0.1.0,<0.2",
+    "google-auth>=2.40,<3",
+]
diff --git a/agent/src/tools/swarm_tool.py b/agent/src/tools/swarm_tool.py
index 0901742..9856bf0 100644
--- a/agent/src/tools/swarm_tool.py
+++ b/agent/src/tools/swarm_tool.py
@@ -704,0 +705 @@ class SwarmTool(BaseTool):
+        base_dir: Path | None = None,
@@ -711,0 +713 @@ class SwarmTool(BaseTool):
+            base_dir: Optional trusted storage directory for this tool's runs.
@@ -714,0 +717 @@ class SwarmTool(BaseTool):
+        self._base_dir = base_dir
@@ -762 +765 @@ class SwarmTool(BaseTool):
-        swarm_base_dir = Path(__file__).resolve().parents[2] / ".swarm" / "runs"
+        swarm_base_dir = self._base_dir or Path(__file__).resolve().parents[2] / ".swarm" / "runs"
